import React from "react";
import { guideIsAvailableForPlace, formatDate } from "../utils";
export default function DestinationCard({item,guideDate}){const guides=guideDate?guideIsAvailableForPlace(item.id,guideDate):[];return <div className="destination-card"><img src={item.image} alt={item.name} onError={e=>e.currentTarget.classList.add("image-fallback")}/><div className="card-body"><div><strong>{item.name}</strong><small><i className="bi bi-geo-alt-fill"/> {item.state}</small></div><span className="rating"><i className="bi bi-star-fill"/> {item.rating}</span></div>{guideDate&&<div className={`place-guide-status ${guides.length?"available":"unavailable"}`}><i className={`bi ${guides.length?"bi-person-check":"bi-person-x"}`}/>{guides.length?`${guides.length} local guide${guides.length>1?"s":""} available`:`No local guide available`}<small>{formatDate(guideDate)}</small></div>}</div>}
